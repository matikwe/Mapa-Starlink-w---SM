name: deploy
on:
  push:
    branches: [ deploy-test ]
jobs:
  deploy-prod:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: [14]

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm i
      - run: npm run build
      - run: npm -v
